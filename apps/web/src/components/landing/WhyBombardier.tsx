export function WhyBombardier() {
  return (
    <section id="about" className="scroll-mt-24 bg-surface py-28">
      <div className="mx-auto max-w-6xl px-5 sm:px-8">
        <h2 className="max-w-2xl font-display text-4xl tracking-tight text-ink sm:text-5xl">
          You cannot tell which resume works if the applications live in three
          places.
        </h2>
        <div className="mt-16 grid gap-12 lg:grid-cols-2 lg:gap-24">
          <p className="max-w-[46ch] text-[15px] leading-relaxed text-muted">
            A week of volume without a denominator is just motion. If the CSV,
            the inbox, and the LinkedIn thread are separate, interview rate is a
            guess.
          </p>
          <p className="max-w-[46ch] text-[15px] leading-relaxed text-ink">
            Bombardier keeps one company record, the resume file you actually
            sent, and the outreach note on that same page. The rate is counted
            from the log.
          </p>
        </div>
      </div>
    </section>
  );
}
